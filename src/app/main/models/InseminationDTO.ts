export class InseminationDTO
{
     inseminationId :number;
    clinicVisitsId ? :number;
    manFathersName : string;
    womanFathersName :string;
   folliclesNumber ?:number;
    signature1 ? :boolean; 
    signature2 ? :boolean; 
   signature3 ? :boolean; 
    doctorSignature? :boolean; 
    doctorLicenseNumber ? :number;
    doctorSignatureAfter ? :boolean; 
     treatmentDescription : string;
     doctorTreatment:string;
     treatmentsTime:string;
}
